import AboutContent from "@/components/store/AboutContent";

export const metadata = {
  title: "About — Chictoria",
  description: "The story behind Chictoria — Korean-inspired bags made with care.",
};

export default function AboutPage() {
  return <AboutContent />;
}
